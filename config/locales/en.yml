en:
  events:
    title: "Events"
    new_event: "New Event"
    show_event: "Show this event"
    recent_events: "Events Added in the Last 30 Days"
    upcoming_events: "Next week's events"
    attributes:
      name: "Name"
      date: "Date"
      description: "Description"
      tickets_quantity: "Tickets Quantity"
      capacity: "Capacity"
  date:
    formats:
      long: "%B %d, %Y"
  errors:
    template:
      header:
        one: "1 error prevented this %{model} from being saved"
        other: "%{count} errors prevented this %{model} from being saved"
      body: "The following errors need to be corrected"
  helpers:
    submit:
      create: "Create %{model}"
      update: "Update %{model}"
      destroy: "Destroy %{model}"
    confirm:
      destroy: "Are you sure you want to destroy %{model}?"